version: "2"

linters:
  settings:
    errcheck:
      exclude-functions:
        - (*net/http.Response).Body.Close
        - (*database/sql.DB).Close

exclusions:
  rules:
    - path: '_test\.go'
      linters:
        - errcheck
