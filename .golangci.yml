linters-settings:
  errcheck:
    exclude-functions:
      - (*net/http.Response).Body.Close
      - (*database/sql.DB).Close

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - errcheck
    - source: "w\.Write\("
      linters:
        - errcheck
