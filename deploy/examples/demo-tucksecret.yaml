apiVersion: tuck.io/v1alpha1
kind: TuckSecret
metadata:
  name: demo-api-key
  namespace: default
spec:
  tuckPath: demo-app/api-key
  secretName: demo-app-credentials
  secretKey: api-key
  refreshInterval: "30s"