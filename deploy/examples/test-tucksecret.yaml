apiVersion: tuck.io/v1alpha1
kind: TuckSecret
metadata:
  name: db-password
  namespace: default
spec:
  tuckPath: myapp/db-password
  secretName: myapp-db-credentials
  secretKey: password
  refreshInterval: "30s"
